{
  "$comment": "Terminal recordings behind the sample install. scripts/capture-demo-sessions.mjs runs every entry with scripts/capture-agent-scrollback.js against a scratch clone of the named repo, then the spawn and Command Terminal boots it derives from the dataset; re-run it when an agent TUI changes. Each entry maps to the session id the dataset replays it into. stopWhen cuts the recording on the first output matching the pattern; stopAfter cuts it at a time; cols and rows override the PTY size, which otherwise comes from geometry: every recording is made at the size of the surface it plays on, so a full-screen TUI reproduces and a row-based one wraps and pads where the desktop would. tiled names a second recording of the same entry at the tiled surface's width, which the seed shows when the session's window mounts narrower than the single recording (a tiled scene); the session's clock, trail and diff stay the single one's. transcript asks for the agent's own transcript beside the recording (transcripts/<file>, derived by main's parsers at record time or by scripts/backfill-demo-transcripts.mjs), which the conversation viewer shows. liveTailMs is how long before its recording's end the live frame opens a session shown as working (the rest streams in after the page opens; a session in demo-dataset.ts can carry its own liveTailMs); the capture script keeps the frame at that moment beside the recording's end (openFrame), and a still paints it for such a session.",
  "liveTailMs": 90000,
  "repos": {
    "contoso-web": { "scaffold": "scripts/demo-repos/contoso-web" },
    "spring-petclinic": { "git": "https://github.com/spring-projects/spring-petclinic.git" },
    "online-boutique": { "git": "https://github.com/GoogleCloudPlatform/microservices-demo.git" }
  },
  "geometry": {
    "$comment": "Terminal sizes at the 1600x1000 site frame, measured by `node demo/measure.mjs --geometry` (it reads the seed's window.__demoNaturalGeometry; `--check` compares each surface against the scale it names). A surface's grid moves with the device scale, because the renderer rounds the terminal cell to device pixels, so `scale` records the launch each surface was measured at. taskWindow and commandTerminal are the window manager's default window (0.58 of the frame) at scale 1, the demo tier's launch, where it fits 154 by 37; at the rig's scale 2 launch the same window fits 142, which is why the floating scenes open at 0.64 (tests/captures/scenes.ts). taskWindowTiled and commandTerminalTiled are one of two panes tiled in the footprint a dock produces, measured at scale 2, the rig's launch, so the rig shows a tiled recording at native type: 115 there, and 125 (task window) or 124 (Command Terminal) at scale 1, where the wider pane holds the recording at native type letterboxed. A session records at taskWindow, a Command Terminal session at commandTerminal, a session with a `tiled` sibling once more at the tiled surface, and a Command Terminal boot at both sizes its window can open at (terminal-<project>.json alone, terminal-<project>-tiled.json beside an existing terminal), chosen at spawn time. The spring-petclinic and online-boutique tiled boots are still at the earlier 124 (Codex, out of credits on the recording machine); a recording carries its own grid, so the frame holds those at 0.93 rather than native until they are re-run. The rows depend on the session's agent: a Claude session's context bar carries the account's rate-limit pills and wraps to two rows, leaving 37; every other agent's bar is one row, leaving 39 (rowsByAgent). A replay cannot renegotiate the size the way a live PTY does.",
    "taskWindow": { "cols": 154, "rows": 37, "scale": 1, "rowsByAgent": { "codex": 39, "gemini": 39, "opencode": 39, "copilot": 39 } },
    "taskWindowTiled": { "cols": 115, "rows": 37, "scale": 2, "rowsByAgent": { "codex": 39, "gemini": 39, "opencode": 39, "copilot": 39 } },
    "commandTerminal": { "cols": 154, "rows": 37, "scale": 1, "rowsByAgent": { "codex": 39, "gemini": 39, "opencode": 39, "copilot": 39 } },
    "commandTerminalTiled": { "cols": 115, "rows": 37, "scale": 2, "rowsByAgent": { "codex": 39, "gemini": 39, "opencode": 39, "copilot": 39 } }
  },
  "captures": [
    { "file": "contoso-web-claude-middleware.json", "tiled": "contoso-web-claude-middleware-tiled.json", "transcript": true, "sessionId": "sess-cw-middleware", "project": "contoso-web", "agent": "claude", "prompt": "Extract the auth logic that is inlined in server/routes.ts into a standalone middleware at server/middleware/auth.ts, use it from the routes, and run npm test when you are done." },
    { "file": "contoso-web-claude-websocket.json", "sessionId": "sess-cw-websocket", "project": "contoso-web", "agent": "claude", "mode": "plan", "prompt": "Plan how to add reconnection with exponential backoff to src/lib/websocket.ts. Read the file and the places that use it, then write the plan. Do not edit any file." },
    { "file": "contoso-web-cursor-integration.json", "sessionId": "sess-cw-integration", "project": "contoso-web", "agent": "cursor", "timeout": 420, "prompt": "Add an integration test for the login route in tests/auth.integration.test.ts that covers a successful login and a wrong password, then run npm test." },
    { "file": "contoso-web-claude-api-client.json", "tiled": "contoso-web-claude-api-client-tiled.json", "sessionId": "sess-cw-api-client", "project": "contoso-web", "agent": "claude", "prompt": "Add typed request and response interfaces for every route in server/routes.ts to a new src/types/api.ts, then make apiFetch in src/lib/http-client.ts generic over them and use the types from src/App.tsx. Run npm test when you are done." },
    { "file": "contoso-web-copilot-rate-limit.json", "sessionId": "sess-cw-rate-limit", "project": "contoso-web", "agent": "copilot", "prompt": "Review server/rate-limit.ts and tests/rate-limit.test.ts for correctness bugs. Report each finding with the line it is on, fix the ones you are sure about, and run npm test." },
    { "file": "contoso-web-claude-terminal.json", "tiled": "contoso-web-claude-terminal-tiled.json", "sessionId": "sess-cw-terminal-1", "project": "contoso-web", "agent": "claude", "prompt": "Summarize what this repository does in three lines, then list the npm scripts it defines." },
    { "file": "spring-petclinic-codex-flaky-tests.json", "sessionId": "sess-pc-flaky-tests", "project": "spring-petclinic", "agent": "codex", "mode": "bypass", "stopAfter": 20, "prompt": "PetClinicIntegrationTests.testOwnerDetails is flaky on the MySQL profile: the visits of an owner come back in a different order about one run in ten. Read the test and the Owner, Pet, and Visit entities, find the ordering that is not deterministic, and fix it in the code. Do not run Maven." },
    { "file": "spring-petclinic-gemini-owner-search.json", "sessionId": "sess-pc-owner-search", "project": "spring-petclinic", "agent": "gemini", "mode": "default", "prompt": "The find-owners form only searches by last name. Add a telephone field to the form and a repository query that finds owners by telephone. Do not run Maven." },
    { "file": "spring-petclinic-opencode-caffeine.json", "sessionId": "sess-pc-caffeine", "project": "spring-petclinic", "agent": "opencode", "stopAfter": 100, "prompt": "Replace the JCache-based cache configuration for the vets list with the Caffeine cache starter, keeping the cache name vets so the actuator endpoint still shows it. Do not run Maven." },
    { "file": "online-boutique-codex-redis-ttl.json", "sessionId": "sess-ob-redis-ttl", "project": "online-boutique", "agent": "codex", "mode": "bypass", "stopAfter": 28, "prompt": "In src/cartservice, carts stored in Redis never expire. Set a TTL on the cart key when a cart is written and refresh it when a cart is read, in RedisCartStore.cs. Do not run dotnet." },
    { "file": "online-boutique-opencode-mtls.json", "sessionId": "sess-ob-mtls", "project": "online-boutique", "agent": "opencode", "prompt": "Add an Istio PeerAuthentication manifest in STRICT mode for the frontend namespace under istio-manifests/, plus the DestinationRule the frontend needs to keep calling the other services with mTLS." },
    { "file": "online-boutique-copilot-currency-a11y.json", "sessionId": "sess-ob-currency-a11y", "project": "online-boutique", "agent": "copilot", "prompt": "The currency selector in src/frontend/templates/header.html has no label, and its select submits the form on every onchange, so choosing a currency with the keyboard reloads the page on each arrow key. Give it a real label, replace the inline onchange submit with an explicit submit control that still works without JavaScript, and give the quantity select in product.html the same label treatment. Do not run go build." },
    { "file": "online-boutique-codex-otel.json", "sessionId": "sess-ob-otel", "project": "online-boutique", "agent": "codex", "mode": "bypass", "prompt": "Add OpenTelemetry tracing to the gRPC server in src/shippingservice/main.go using the otel Go SDK, exporting to the collector address from the COLLECTOR_ADDR environment variable. Do not run go build." },
    { "file": "contoso-web-claude-empty-states.json", "sessionId": "sess-cw-empty-states", "project": "contoso-web", "agent": "claude", "mode": "plan", "prompt": "Onboarding empty states: First-run screens for the dashboard, projects, and billing pages before any data exists" },
    { "file": "contoso-web-claude-vite8.json", "sessionId": "sess-cw-vite8", "project": "contoso-web", "agent": "claude", "mode": "acceptEdits", "prompt": "Upgrade to Vite 8: Move the build to Vite 8 and drop the legacy Rollup plugins. Do not run npm install." },
    { "file": "spring-petclinic-claude-java-25.json", "sessionId": "sess-pc-boot-35", "project": "spring-petclinic", "agent": "claude", "mode": "acceptEdits", "prompt": "Upgrade the toolchain to Java 25: java.version in pom.xml, the GitHub Actions matrix, and the Dockerfile base image. Do not run Maven." }
  ]
}